"use strict";
const favourite_button = document.getElementById("heart");

function activate_favourites(event) {

    if (favourite_button.addEventListener)


};


favourite_button.addEventListener("clikc", activate_favourites);